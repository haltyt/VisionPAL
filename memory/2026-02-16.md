# 2026-02-16

## VisionPAL Umwelt — 認知パイプライン完成！

### やったこと
- README更新（Umweltコンセプト、「AIロボットは人間の夢を見るか？」サブタイトル）
- StreamDiffusion README更新（venv + TensorRT、conda不要）
- JetBot起動: mjpeg_perception.py + mqtt_robot.py + collision_detect.py
- **mjpeg_server.py → USBカメラ対応版に切り替え**（mjpeg_lightはCSI専用旧版）
- **collision_detect.py改修**: CSIカメラ直接 → MJPEG経由に変更（カメラ競合なし）+ MQTT publish追加
- **mjpeg_perception.py デプロイ**: USBカメラ + ResNet SSD顔検出 + MQTT publish
  - MobileNet SSD(20クラス)はprototxt/caffemodel互換性問題で断念
  - blobFromImage前処理: 顔検出は `scale=1.0, mean=(104,177,123)` ← MobileNet用の `0.007843, 127.5` だと検出不可
  - confidence 0.985-0.992で安定検出
- **Cognition Engine修正**:
  - `self.config` → `cfg` 修正
  - MQTT subscribe追加（perception/objects, perception/collision）
  - TTS再生: BT自動再接続付き（pactl set-default-sink + bluetoothctl connect）
  - speak()デバッグログ追加
  - プロンプト短縮（CLIP 77トークン制限対応）
  - 感情スタイル・記憶ビジュアル記述を簡潔化
- **全パイプライン動作確認**: カメラ→顔検出→MQTT→感情変化→プロンプト→StreamDiffusion→TTS→BTスピーカー

### 技術メモ
- JetBotのnohup起動: `PYTHONUNBUFFERED=1` 必須（stdoutバッファリングでログ見えない）
- BTスピーカー(LSPX-S1)はすぐ切れる → TTS再生時にpactl確認+自動再接続
- Cognition Engineバックグラウンド起動: `nohup`だとプロセスが即死するケースあり → `&`で直接起動
- JetPackにSSD-Mobilenet-v2(UFF)あるが、jetson-inferenceのPythonバインディング未ビルド
- deploy.prototxt(顔検出ResNet SSD): `/home/jetbot/models/deploy.prototxt` + `res10_300x300_ssd_iter_140000.caffemodel`

### git
- VisionPAL @ `2b2cb21` — perception pipeline complete
